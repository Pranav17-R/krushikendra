{
  "name": "krushikendra-root",
  "version": "1.0.0",
  "description": "Root orchestration for Trishul Krushi Kendra (frontend + backend)",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend",
    "install:all": "npm install --prefix backend",
    "postinstall": "npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
